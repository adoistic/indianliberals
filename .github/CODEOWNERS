# CODEOWNERS for indianliberals
# https://docs.github.com/en/repositories/managing-your-repositories-settings-and-features/customizing-your-repository/about-code-owners
#
# Files listed here require approval from the designated reviewers before
# a pull request can be merged. This is the GitHub-side enforcement of the
# super-admin / sub-admin trust model.
#
# Editor access is enforced at runtime by the Thothica CMS (Firebase roles
# in Firestore; see apps/cms). This file is the git-time enforcement for
# the pieces that shape every editor's workflow.
#
# Currently:
#   adoistic        — Adnan

# Branch-protection-sensitive config: only super-admins approve.
/.github/CODEOWNERS                           @adoistic

# Content schema and routing — affect every editor's workflow, so changes
# here also need super-admin sign-off (we can relax this if needed).
/apps/site/src/content.config.ts              @adoistic
/apps/site/src/layouts/                       @adoistic
