{
  "name": "@indianliberals/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers OAuth proxy for Sveltia CMS — enforces the email allowlist on a public repo. Super-admins hardcoded in source; sub-admins loaded from data/admins.json in the repo.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251002.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.49.0"
  }
}
