{
  "name": "@indianliberals/theprint-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: daily cron that ingests new articles from ThePrint's 'Indian Liberals Matter' RSS feed, commits them as theprint-mirror markdown files via the GitHub Contents API. Preserves admin edits (Critical Gap T20) and respects the blocklist.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "types": "wrangler types",
    "ingest:repo": "tsx scripts/ingest-to-repo.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251002.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.49.0"
  }
}
