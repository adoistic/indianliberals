// Which opinion entries are events rather than argument.
//
// Shared so /events/ and /opinions/ can never disagree about it. They are two
// halves of one split (CCS round-3 feedback, 7.3): whatever Events lists,
// Opinions must not, and a second copy of this string in the other file is
// exactly how that drifts.

export const EVENT_KIND = "event-coverage";
